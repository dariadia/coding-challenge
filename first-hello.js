// From https://jscodebox.com/challenge/first-hello
// A string sentence is given. Return true if the block starts with "Hello".

function firstHello(sentence) {
  return sentence.indexOf("Hello") === 0
}
